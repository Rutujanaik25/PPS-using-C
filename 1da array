#include <stdio.h>

int main()
{
    
    int arr[5] = { 1, 2, 4, 8, 16 };

   printf("enter number of the array:\n");
    for (int i = 0; i < 5; i++)
    {
        printf("element %d: %d\n", i, arr[i]);
    }

    return 0;
}
